# Nixpacks config for Railway deployment
# Pins Node.js to v22 (v18 reached EOL and was removed)

[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["bun install"]

[start]
cmd = "bun run agents/worker/index.ts"
